
export const DEFAULT_ARRAY_SIZE = 20;
export const DEFAULT_TREE_SIZE = 12;
export const MIN_ARRAY_SIZE = 5;
export const MAX_ARRAY_SIZE = 50;
export const ANIMATION_SPEED_MS = 400; // Default to 4x (where 1x = 1600ms)

// Tree specific
export const TREE_ROOT_Y = 10;
export const TREE_LAYER_HEIGHT = 15;

// Graph specific
export const GRAPH_NODE_RADIUS = 20;

export const THEME_COLORS = {
  primary: 'bg-indigo-500',
  secondary: 'bg-indigo-100',
  accent: 'bg-teal-400',
  danger: 'bg-rose-400',
  sorted: 'bg-emerald-400',
  comparing: 'bg-amber-300',
};
